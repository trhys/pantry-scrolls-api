START OF LOG - 4/30/26
----------------------------
Commit From: 0775209

# SQL: 

Deleted the shopping_list_ingredients join table and instead we now just use the PrintList query to fetch all the necessary stuff from the database. Did some general clean up in a few areas also.

+2 migrations - added foreign key constraint to ingredients referencing units(name); dropped shopping_list_ingredients;

# Internal: 

Added new viewmodel package which pulls response structure out of the http handlers and instead generates them from database objects passed in from the handler.

Added data package which is responsible for database seeding and integrity. On server startup we are checking the database ingredients, units, retail_units, conversions tables so verify they contain all the entries from setup.json

# App:

Major frontend changes concerning the JS structure in general. Not a ton of code changed but we have it better organized instead of the big long app.js file every page had to load. Will continue to polish over time.

# Misc:

Added middleware for auth onto http handlers. Also pulled repetitve decoder stuff into a helper that can grow without causing a cascade of edits. Removed some endpoints that were functionally redundant and modified existing endpoints to be more RESTy.

Working on the README still. 

Dockerfile saw an edit due to moving setup.json into the internal directory, so its not getting copied in the image anymore.

END:
-----------------------------

5/4/26

Commit From: 2490bf0

# SQL:

Added instructions column to the recipes table. This is another text field that will hold user steps for the recipe, separate from the description. Will add length limits along the chain

# Internal:

Made changes to viewmodels package to accomodate the new table, but alos added some finer degrees of separation betweenvisible data between endpoints.

# App:

Added to the recipe creator form to allow user input into the instructions table. Same changes went out to the api.js file, and the template for recipes had it added in as well.

END:
-----------------------------

5/6/26

Commit From: 28c6dc9

# Internal:

Added a new seeding function on server start to populate some recipes.

# SQL:

Squashed migration history.

# Main:

Made some changes to error logging to make server side messages more informative, and response messages more consistent.

Added update and delete endpoints for recipes.

Removed user_id from handlerCreateRecipe() request shape.

END:
-------------------------------

